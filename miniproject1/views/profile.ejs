<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
  </head>

  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
      <div class="flex justify-end w-full">
        <a
          class="bg-red-500 mb-5 rounded-md px-3 py-2 mb-5 inline-block text-sm"
          href="/logout"
        >
          Logout</a
        >
      </div>
      <div class="flex items-start gap-3">
        <div class="w-10 h-10 rounded-md overflow-hidden">
          <img
            class="w-full h-full object-cover"
            src="/images/uploads/<%= user.profilepic  %>"
            alt=""
          />
        </div>
        <h1 class="text-3xl mb-4">
          <span class="font-light"> Hello</span>, <%= user.name %> 😃✌️
        </h1>
      </div>
      <h5 class="mb-5">You Can Create a New Post</h5>
      <form action="/post" method="post">
        <textarea
          name="contant"
          class="w-1/3 block resize-none bg-transparent border-2 border-zinc-800 rounded-lg p-3 outline-none"
          placeholder="What's On Your Mind"
        ></textarea>
        <input
          class="px-3 py-2 bg-blue-500 block rounded-lg mt-2 w-40"
          type="submit"
          value="Create New Post"
        />
      </form>
      <div class="posts mt-20">
        <h3 class="text-2xl text-zinc-400">Your Posts</h3>
        <div class="post container mt-5">
          <% user.post.reverse().forEach((post)=> { %>
          <div
            class="post w-1/3 p-4 mb-3 rounded-md bg-zinc-800 border-2 border-zinc-700"
          >
            <h4 class="text-blue-500 mb-2">@<%= user.username %></h4>
            <p class="text-sm tracking-tight"><%= post.contant %></p>

            <small class="mt-2 inline-block">
              <%= post.likes.length %>Likes
            </small>

            <div class="btn flex gap-5 mt-5">
              <a class="text-blue-400" href="/like/<%= post._id %>">
                <%= post.likes.indexOf(user._id)===-1 ? "like" : "Unlike" %>
              </a>
              <a class="text-zinc-500" href="/edit/<%= post._id  %>">Edit</a>
            </div>
          </div>

          <% }) %>
        </div>
      </div>
    </div>
  </body>
</html>
