<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="flex justify-end w-full">
            <a class="bg-red-500 mb-5 rounded-md px-3 py-2 mb-5 inline-block text-sm" href="/logout">
                Logout</a>
        </div>
       
        <h5 class="mb-5">Edit Your Post</h5>
        <form action="/update/<%= post._id  %>" method="post">
            <textarea name="contant"
                class="w-1/3 block resize-none bg-transparent border-2 border-zinc-800 rounded-lg p-3 outline-none"
                placeholder="What's On Your Mind"><%= post.contant  %></textarea>
            <input class="px-3 py-2 bg-yellow-500 text-green-900 block rounded-lg mt-2 w-40" type="submit" value="Update Post" />
        </form>
       
    </div>
</body>

</html>