<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/stylesheet/style.css" />
    <title>Document</title>
  </head>
  <body>
    <div class="bg-zinc-800 w-full h-full text-white p-10">
      <div class="nav mb-7">
        <a class="text-blue-400" href="/"> Go to Home</a>
      </div>
      <h1 class="text-3xl tracking-tighter">All Users</h1>
      <div class="users flex gap-3 flex-wrap">
        <% if (users.length >0) { %> <% users.forEach(user => { %>
        <div class="user w-72 p-4 bg-zinc-700 rounded">
          <div class="w-full h-52 rounded-lg bg-zinc-500">
            <img
              class="w-full h-full object-cover overflow-hidden"
              src="<%= user.image  %>"
              alt=""
            />
          </div>
          <h3 class="text-2xl tracking-tighter text-center mt-3">
            <%= user.name %>
          </h3>
          <h5 class="text-zinc-500 text-center"><%= user.email %></h5>
          <div class="flex gap-3 mt-10">
            <a class="text-zinc-300" href="/edit/<%= user._id  %>"> Edit User</a>
            <a class="text-red-500" href="/delete/<%= user._id %>">
              Delete User</a
            >
          </div>
        </div>

        <% }) %> <% } else { %>
        <h3 class="text-zinc-500 text-3xl mt-6">No users yet</h3>
        <% } %>
      </div>
    </div>
  </body>
</html>
